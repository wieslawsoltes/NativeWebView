[Added] Added real per-instance proxy application for macOS `NativeWebView` and `NativeWebDialog` instances using dedicated `WKWebsiteDataStore` proxy configurations on macOS 14+.
[Added] Added `NativeWebViewFeature.ProxyConfiguration` and proxy resolution helpers so runtime support can be surfaced explicitly.
[Added] Added coverage for proxy parsing, platform support surfacing, and dialog instance-configuration forwarding.
[Docs] Documented the actual per-instance proxy support matrix across macOS, Windows, Linux, iOS, Android, and Browser targets.
